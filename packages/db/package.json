{
  "name": "@optimeleon/db",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "prepare": "echo 'DATABASE_URL=\"postgres://postgres:password@localhost:5432/mydb\"' > .env && pnpm run migrate --name init && pnpm run generate",
    "reset": "rimraf prisma/migrations && rimraf generated && pnpm prisma migrate reset --force",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio",
    "generate": "prisma generate",
    "dev": "pnpm studio",
    "build": "pnpm generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prisma": "^6.13.0",
    "@optimeleon/eslint": "workspace:*",
    "@optimeleon/tsconfigs": "workspace:*"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0"
  }
}
