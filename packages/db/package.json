{
  "name": "@optimeleon/db",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.cjs",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepare": "echo 'DATABASE_URL=\"postgres://postgres:password@localhost:5432/mydb\"' > .env",
    "reset": "rimraf prisma/migrations && rimraf generated && pnpm prisma migrate reset --force",
    "migrate": "prisma migrate dev",
    "studio": "prisma studio",
    "generate": "prisma generate",
    "dev": "pnpm studio",
    "prebuild": "pnpm run migrate --name pre-build && pnpm run generate",
    "build": "rollup -c rollup.config.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prisma": "^6.13.0",
    "@optimeleon/eslint": "workspace:*",
    "@optimeleon/tsconfigs": "workspace:*",
    "@optimeleon/rollup": "workspace:*"
  },
  "dependencies": {
    "@prisma/client": "^6.13.0"
  }
}
